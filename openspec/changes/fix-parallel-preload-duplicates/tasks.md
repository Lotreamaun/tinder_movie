# Tasks

## 1. Транзакционные границы (deck_service)

- [x] 1.1 Убрать `db.commit()` и `db.refresh()` из create-ветки `_get_or_create_position` (вставка позиции остаётся незакоммиченной; финальный commit делает `get_next_movie_for_room`)
- [x] 1.2 Убрать `db.commit()` из `_replenish_deck` — транзакция держится до финального commit внешнего метода
- [x] 1.3 Убедиться, что финальный `db.commit()` в `get_next_movie_for_room` атомарно персистит insert позиции и advance (position = index + 1); при необходимости поправить докстринги методов

## 2. Тест гонки

- [x] 2.1 Добавить тест в `test_shared_deck.py`: участник + ≥ N фильмов, позиция заранее НЕ создаётся; N потоков с отдельными `SessionLocal`, синхронизированных `threading.Barrier` перед вызовом, запрашивают фильм; ассерт — N уникальных фильмов
- [x] 2.2 Прогнать `python3 -m pytest tests -q` в `backend/` — все тесты зелёные (включая прежние 5.x/6.x)
- [x] 2.3 Проверить red→green: до фикса (п. 1) новый тест стабильно падает (дубликаты), после — проходит

## 3. Верификация

- [x] 3.1 API-проверка: 5 параллельных запросов `GET /api/movies/random?room_code=...` с заголовком `telegram-id` возвращают разные фильмы (или перезапустить repro-скрипт из /tmp — 5 уникальных)
- [ ] 3.2 Ручная проверка в Telegram miniApp: карточка сменяется с первого же свайпа/нажатия (не с 5-го).
  - Диагноз по логам 12.08 03:58: бэкенд исправен (5 параллельных GET и серия свайпов через HTTP возвращают разные фильмы), а наблюдаемое поведение («та же карточка 10 свайпов», свайп фильма не из колоды 99EXZL, группа из 3 участников, 1 GET на инициализацию) невозможно для текущего кода+данных — miniApp выполняет устаревший JS-бандл, закэшированный WebView Telegram (HMR Vite не доходит через ngrok).
  - Проверить: жёсткая перезагрузка miniApp в Telegram (очистка WebView-кэша), затем ретест. Также можно проверить локально в браузере на http://localhost:5173 (localStorage telegramId=25293232).
