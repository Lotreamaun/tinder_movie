## 1. Модель данных

- [ ] 1.1 Alembic-миграция: таблица `room_decks` (room_code PK FK→rooms.id ondelete CASCADE, movie_ids JSONB, created_at)
- [ ] 1.2 SQLAlchemy-модель `RoomDeck` в `backend/app/models/`

## 2. Сервис колода

- [ ] 2.1 `movie_service`/новый `deck_service`: создание колода (перемешанная пачка из movies, до N=50, без уже свайпнутых в комнате)
- [ ] 2.2 Выбор «следующего фильма» участника: первый существующий id колода, не свайпнутый этим пользователем в комнате (anti-join `user_swipes`)
- [ ] 2.3 Пополнение при исчерпании: новая пачка, исключая свайпнутые в комнате и уже присутствующие id; защита от дублей
- [ ] 2.4 Устойчивость к ротации: пропуск несуществующих фильмов на выдаче
- [ ] 2.5 Конкурентная безопасность: row lock / идемпотентное создание и пополнение колода

## 3. API

- [ ] 3.1 `GET /api/movies/random` принимает контекст комнаты (`room_code`), сверяется с личностью пользователя
- [ ] 3.2 Вне комнаты — прежнее случайное поведение; ответ сохраняет схему `MovieResponse`
- [ ] 3.3 Схема ответа/документация эндпоинта обновлены (пример с room_code)

## 4. Фронтенд

- [ ] 4.1 `api.ts`: передача `room_code` в запрос случайного фильма
- [ ] 4.2 `App.tsx`: использование комнаты из `GET /api/rooms/my` (room_code) при загрузке фильмов; без комнаты — случайный фильм
- [ ] 4.3 Проверка: участники комнаты видят фильмы из одного колода

## 5. Тесты

- [ ] 5.1 pytest: участники комнаты получают фильмы из общей последовательности (mock Kinopoisk)
- [ ] 5.2 pytest: без повторов для участника (после свайпа фильм не выдаётся снова)
- [ ] 5.3 pytest: пополнение при исчерпании без уже свайпнутых фильмов
- [ ] 5.4 pytest: пропуск удалённых фильмов колода
- [ ] 5.5 pytest: матч возможен — все участники комнаты лайкнули один фильм из общего колода → создаётся матч
- [ ] 5.6 pytest: вне комнаты поведение прежнее (случайный фильм)
