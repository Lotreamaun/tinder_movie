## 1. Конфиг

- [x] 1.1 Добавить в `Settings` (`backend/app/config.py`) `KINOPOISK_COLLECTIONS` (список, env через запятую, по умолчанию `TOP_250_MOVIES,TOP_POPULAR_MOVIES`), `CATALOG_GROWTH_MAX_PAGES = 15` и `CATALOG_SOURCES_EXHAUSTED_COOLDOWN_HOURS = 168` с комментариями

## 2. Источник Kinopoisk

- [x] 2.1 Вынести маппинг данных фильма Kinopoisk → поля `Movie` из `fetch_movie_from_kinopoisk(full_data=True)` в приватную функцию; `fetch_movie_from_kinopoisk` использует её без изменения поведения
- [x] 2.2 Реализовать выборку страницы подборки `/api/v2.2/films/collections?type=&page=`: возвращает смапленные новые фильмы страницы (известные по `kinopoisk_id` и не-`FILM` отсеяны) и `totalPages`; при ошибке запроса или 402 — признак сбоя, как сейчас (402 одной строкой WARNING)
- [x] 2.3 Удалить `get_top_movies_from_kinopoisk` и его использования

## 3. Проход по подборкам

- [x] 3.1 Добавить в `movie_service` состояние прохода в памяти `(collection_index, next_page, new_in_pass)` с комментарием, что доступ сериализован `_growth_lock` в `deck_service`
- [x] 3.2 Ввести результат попытки (dataclass): `loaded`, `stop_reason` (`count` | `budget` | `pass_end` | `error`), `new_in_pass` для `pass_end`
- [x] 3.3 Переписать `load_batch_movies`: стартует с курсора, идёт по страницам до `totalPages` (или пустого `items`), переходит к следующей подборке, накапливает `new_in_pass`; останавливается по `count`, бюджету `CATALOG_GROWTH_MAX_PAGES`, концу прохода (курсор → начало, `new_in_pass` возвращается и обнуляется, новый проход в той же попытке не начинается) или ошибке (курсор не сдвигается за проблемную страницу); убрать лимит `page > 10`
- [x] 3.4 Логирование: строка INFO на страницу `collection/page/totalPages/new` и итог попытки с `stop_reason`

## 4. Пауза догрузки

- [x] 4.1 В `deck_service._run_background_growth` выбирать паузу по результату (design.md, Decision 6): `error`/исключение — короткая; `pass_end` с `new_in_pass == 0` — длинная; остальное — без паузы
- [x] 4.2 `_set_growth_cooldown` принимает длительность и причину; лог `Catalog growth cooldown started reason=... until=...`
- [x] 4.3 Обновить `_grow_catalog` и строку `Grew catalog by ...` под новый результат `load_batch_movies`

## 5. Тесты

- [x] 5.1 Переделать `test_load_batch_movies_continues_past_fully_known_page` и тесты `test_shared_deck.py`, подменяющие `load_batch_movies`, под новый источник и результат
- [x] 5.2 Тест: подборка из 13 страниц, первые 10 известны — новые фильмы со страниц 11–13 загружаются (потолка в 10 страниц нет)
- [x] 5.3 Тест: подборка исчерпана — в той же попытке догрузка переходит к следующей подборке
- [x] 5.4 Тест: бюджет страниц исчерпан — следующая попытка продолжает со следующей страницы
- [x] 5.5 Тест: конец прохода — попытка останавливается на последней странице последней подборки, не начиная новый проход; следующая попытка начинает с первой страницы первой подборки
- [x] 5.6 Тест: `new_in_pass` суммируется по нескольким попыткам одного прохода и обнуляется на конце прохода
- [x] 5.7 Тест: элементы с `type != FILM` не попадают в каталог; для новых фильмов не делается отдельный запрос полных данных
- [x] 5.8 Тесты пауз в `deck_service`: `budget` без новых фильмов — паузы нет; `error` — короткая; пустой полный проход — длинная (новая попытка до её истечения не стартует); полный проход с новыми фильмами — паузы нет
- [x] 5.9 Прогнать `pytest` целиком

## 6. Проверка вручную

- [x] 6.1 Запустить бэкенд с реальным ключом, довести `remaining` ниже порога и убедиться по логам, что догрузка добавила фильмы 201–250 из `TOP_250_MOVIES`, а следующие попытки продолжают проход без 30-минутных пауз между ними
