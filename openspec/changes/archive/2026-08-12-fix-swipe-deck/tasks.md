## 1. Backend: курсор позиции участника в колоде

- [x] 1.1 Создать модель `RoomDeckPosition` (`room_deck_positions`, PK `(user_id, room_code)`, поле `position`) в `backend/app/models/` и зарегистрировать её в базе
- [x] 1.2 Создать Alembic-ревизию: таблица `room_deck_positions` с FK на `users.id` и `rooms.id` (CASCADE)
- [x] 1.3 В `deck_service.py`: метод создания/чтения строки позиции с `SELECT ... FOR UPDATE` и обработкой `IntegrityError` при гонке (по образцу `_get_or_create_deck`)
- [x] 1.4 В `deck_service.py`: переписать `get_next_movie_for_room` на алгоритм prune → clamp → scan → advance (см. design.md D3): prune удалённых фильмов, clamp позиции, scan от позиции с пропуском просвайпанных, сохранение `position = index_found + 1`
- [x] 1.5 В `deck_service.py`: при достижении конца колода (позиция за пределами `movie_ids`) пополнять колод через существующий `_replenish_deck` и продолжать scan; возвращать `None`, если фильма нет
- [x] 1.6 Дополнить `backend/tests/test_shared_deck.py`: последовательные запросы одного участника возвращают разные фильмы в порядке колода; 5 параллельных запросов возвращают 5 разных фильмов; просвайпанный фильм пропускается; удалённый фильм (ротация) пропускается без повторов; позиции двух участников независимы; пополнение при исчерпании

## 2. Frontend: адаптивная карточка фильма

- [x] 2.1 В `MovieCard.tsx`: заменить `w-full max-w-md aspect-[2/3] max-h-full` на размер от высоты — `h-full aspect-[2/3] max-w-full` (см. design.md D4)
- [x] 2.2 В `SkeletonCard.tsx`: синхронизировать геометрию с карточкой (те же классы размера)

## 3. Frontend: догрузка очереди без заглушки

- [x] 3.1 В `App.tsx`: при исчерпании предзагруженной очереди в `handleSwipe` показывать `SkeletonCard` (флаг загрузки следующего фильма), а заглушку «Нет доступных фильмов» — только если догрузка вернула пусто/ошибку

## 4. Проверка

- [x] 4.1 Прогнать backend-тесты: `pytest backend/tests` — новые и существующие (`test_shared_deck.py`) зелёные
- [x] 4.2 Прогнать `npm run build` и `npm run lint` в `frontend/` — без ошибок
- [x] 4.3 Ручная проверка в Telegram на локалхосте (ngrok): старт показывает карточки разных фильмов, свайп/кнопка сразу меняют карточку, «No movies available» не появляется, карточка заполняет экран с пропорцией 2:3
