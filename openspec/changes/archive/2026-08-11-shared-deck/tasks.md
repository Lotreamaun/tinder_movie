## 1. Модель данных

- [x] 1.1 Alembic-миграция: таблица `room_decks` (room_code PK FK→rooms.id ondelete CASCADE, movie_ids JSONB, created_at)
- [x] 1.2 SQLAlchemy-модель `RoomDeck` в `backend/app/models/`

## 2. Сервис колода

- [x] 2.1 `movie_service`/новый `deck_service`: создание колода (перемешанная пачка из movies, до N=50, без уже свайпнутых в комнате)
- [x] 2.2 Выбор «следующего фильма» участника: первый существующий id колода, не свайпнутый этим пользователем в комнате (anti-join `user_swipes`)
- [x] 2.3 Пополнение при исчерпании: новая пачка, исключая свайпнутые в комнате и уже присутствующие id; защита от дублей
- [x] 2.4 Устойчивость к ротации: пропуск несуществующих фильмов на выдаче
- [x] 2.5 Конкурентная безопасность: row lock / идемпотентное создание и пополнение колода

## 3. API

- [x] 3.1 `GET /api/movies/random` принимает контекст комнаты (`room_code`), сверяется с личностью пользователя
- [x] 3.2 Вне комнаты — прежнее случайное поведение; ответ сохраняет схему `MovieResponse`
- [x] 3.3 Схема ответа/документация эндпоинта обновлены (пример с room_code)

## 4. Фронтенд

- [x] 4.1 `api.ts`: передача `room_code` в запрос случайного фильма
- [x] 4.2 `App.tsx`: использование комнаты из `GET /api/rooms/my` (room_code) при загрузке фильмов; без комнаты — случайный фильм
- [x] 4.3 Проверка: участники комнаты видят фильмы из одного колода

## 5. Тесты

- [x] 5.1 pytest: участники комнаты получают фильмы из общей последовательности (mock Kinopoisk)
- [x] 5.2 pytest: без повторов для участника (после свайпа фильм не выдаётся снова)
- [x] 5.3 pytest: пополнение при исчерпании без уже свайпнутых фильмов
- [x] 5.4 pytest: пропуск удалённых фильмов колода
- [x] 5.5 pytest: матч возможен — все участники комнаты лайкнули один фильм из общего колода → создаётся матч
- [x] 5.6 pytest: вне комнаты поведение прежнее (случайный фильм)
