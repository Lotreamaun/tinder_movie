## 1. Догрузка каталога из отдельной сессии (deck_service)

- [x] 1.1 Добавить в `deck_service.py` метод `_grow_catalog(user, room) -> int`, вызывающий `movie_service.load_batch_movies` на отдельной `SessionLocal()` (не на сессии запроса) и возвращающий число реально добавленных фильмов; сессия закрывается в `finally` независимо от результата.
- [x] 1.2 В `get_next_movie_for_room`: если после `_replenish_deck` (пополнение из существующего каталога) `_next_available` вернул `None` — вызвать `_grow_catalog`, затем повторить `_replenish_deck` + `_next_available` ещё раз (Decision 2, design.md).
- [x] 1.3 Убедиться, что вызов `_grow_catalog` не выполняется, пока держится `FOR UPDATE` на deck/position дольше необходимого — сверить план с design.md Decision 1 (блокировки текущей транзакции не должны попадать в тот же вызов, что и HTTP к Kinopoisk).

## 2. Fallback без повтора просвайпанного

- [x] 2.1 В `_random_movie` добавить параметр `exclude: Set[UUID]` (переданный `swiped` из `get_next_movie_for_room`) и исключать эти id из `SELECT ... ORDER BY random() LIMIT 1`, если после исключения остаётся хотя бы один фильм; иначе — прежнее поведение без исключения.
- [x] 2.2 Обновить вызов `_random_movie` в `get_next_movie_for_room`, передав `swiped`.

## 3. Логирование пути выдачи

- [x] 3.1 Добавить `logger.info` с путём выдачи (`deck` / `replenished-from-catalog` / `replenished-after-fetch` / `random-fallback`), `room_code`, `user_id` в соответствующих ветках `get_next_movie_for_room`.

## 4. Тесты

- [x] 4.1 В `test_shared_deck.py`: тест, что при исчерпании колода и каталога `deck_service` вызывает догрузку (замокать `movie_service.load_batch_movies` или HTTP-клиент Kinopoisk) и возвращает новый (не ранее показанный) фильм вместо повтора.
- [x] 4.2 Тест, что при неудачной/пустой догрузке (нет `KINOPOISK_API_KEY` или Kinopoisk не вернул фильмов) фильм всё равно возвращается через `_random_movie`, и по возможности не совпадает с уже просвайпанными участником (пока в каталоге есть альтернативы).
- [x] 4.3 Тест, что вызов `_grow_catalog` не держит и не требует `FOR UPDATE` текущей транзакции — например, конкурентный запрос другого участника комнаты не блокируется на время (замокать долгий HTTP-вызов и проверить, что параллельный запрос к деке/позиции проходит).
- [x] 4.4 Прогнать `pytest` в `backend/` — все тесты (существующие + новые) зелёные.

## 5. Верификация

- [x] 5.1 Ручная проверка: комната с малым исходным каталогом (или искусственно просвайпать большинство фильмов), убедиться, что вместо видимых повторов подгружаются новые фильмы из Kinopoisk (по логам пути выдачи из задачи 3.1).

> **Ревизия плана (после 5.1):** ручная проверка вскрыла, что синхронный вызов `_grow_catalog` внутри запроса (задачи 1.2, 1.3, путь `replenished-after-fetch` из 3.1) держит HTTP-ответ до ~88с — фронтенд обрывает его по таймауту 10с. Разделы 6–9 ниже заменяют этот синхронный путь на пороговый фоновый запуск и добавляют концепцию сессии комнаты (design.md, Decisions 1, 2, 5 — пересмотрены).

## 6. Пороговая фоновая догрузка каталога (заменяет синхронный вызов из задач 1.2/1.3)

- [x] 6.1 Добавить в `config.py` настройку `DECK_CATALOG_LOW_THRESHOLD: int = 30` — порог непросвайпанных (в текущей сессии) фильмов участника, при котором стартует фоновая догрузка.
- [x] 6.2 В `deck_service.py` добавить in-process guard (`threading.Lock` + флаг «догрузка идёт»), защищающий от параллельного запуска нескольких фоновых догрузок одновременно (design.md, Decision 1).
- [x] 6.3 В `get_next_movie_for_room`: сразу после вычисления session-aware `swiped` (раздел 7) посчитать остаток непросвайпанных фильмов участника; если меньше `DECK_CATALOG_LOW_THRESHOLD` и guard свободен — запустить `_grow_catalog` в фоновом потоке (`threading.Thread(..., daemon=True).start()`), не дожидаясь результата (design.md, Decision 2).
- [x] 6.4 Убрать из `get_next_movie_for_room` синхронное ожидание `_grow_catalog` и повторный `_replenish_deck` после неё (старая логика задачи 1.2) — при исчерпании колода и каталога сразу переходить к `_random_movie` (фоновая догрузка, если ещё не шла, запускается по порогу из 6.3, а не здесь).
- [x] 6.5 Обновить логирование путей выдачи: убрать `replenished-after-fetch` (больше не возникает синхронно), добавить отдельную лог-строку о запуске фоновой догрузки (`room_code`, `user_id`, остаток фильмов) — design.md, Decision 4.

## 7. Сессия комнаты (скользящее окно 24ч)

- [x] 7.1 Добавить хелпер вычисления `session_start` для группы участников (SQL с `LAG()` по `user_swipes`, фильтр `group_participants @>` — design.md, Decision 5). Разместить рядом со связанной логикой (`deck_service.py` или новый небольшой модуль, если переиспользуется из `swipe_service.py` — избегать циклического импорта).
- [x] 7.2 Обновить `deck_service._swiped_movie_ids` и `_swiped_movie_ids_in_room` — добавить условие `swiped_at >= session_start`; если сессия истекла (с последнего свайпа группы прошло ≥24ч или свайпов не было) — возвращать пустое множество.
- [x] 7.3 Обновить `swipe_service.check_match` — добавить тот же фильтр по `session_start` для группы участников.
- [x] 7.4 Залогировать факт истечения и сброса сессии (`room_code`, сколько времени было простоя) при обнаружении пустой текущей сессии.

## 8. Тесты

- [x] 8.1 Тест: остаток непросвайпанных (в сессии) фильмов участника ниже порога → запускается фоновая догрузка, и ответ `get_next_movie_for_room`/`GET /api/movies/random` возвращается быстро, не дожидаясь её завершения (замокать медленный `movie_service.load_batch_movies`, например через `time.sleep`, и проверить время ответа).
- [x] 8.2 Тест: повторный вызов, пока предыдущая фоновая догрузка ещё не завершилась, не запускает вторую параллельную догрузку (in-process guard) — например, счётчик вызовов замоканного `load_batch_movies` не растёт, пока первый вызов не завершился.
- [x] 8.3 Тест: свайп со `swiped_at` старше 24 часов от «текущего момента», без более поздних свайпов группы, не входит в исключение — фильм может быть показан повторно (сессия истекла).
- [x] 8.4 Тест: цепочка свайпов группы с разрывами < 24ч друг от друга — все продолжают учитываться как одна (текущая) сессия, несмотря на то что первый свайп цепочки был давно.
- [x] 8.5 Тест: `swipe_service.check_match` не засчитывает лайк из истёкшей (≥24ч простоя) сессии, но засчитывает лайк из текущей.
- [x] 8.6 Тест: сброс сессии (истечение) не удаляет и не изменяет уже существующие записи в `matches`.
- [x] 8.7 Прогнать весь `pytest` в `backend/` — все тесты (существующие + новые) зелёные.

## 10. Рецикл колода после исчерпания новых фильмов (design.md, Decision 6)

- [x] 10.1 `_replenish_deck`: если пополнение новыми фильмами (не из колода) пусто — пополнить фильмами, не просвайпанными участником в текущей сессии, исключив последние `DECK_BATCH_SIZE` элементов колода перед курсором (защита от дублей предзагрузки).
- [x] 10.2 Тест: после истечения сессии и исчерпания колода оба участника получают старые фильмы через пополнение в одном общем порядке, и матч на них достижим.
- [x] 10.3 Тест: рецикл не возвращает недавно выданные участнику (в окне перед курсором), но ещё не свайпнутые фильмы.
- [x] 10.4 Прогнать весь `pytest tests` — зелёный.

> **Ревизия плана (после 9.1):** ручная проверка с реальным ключом исчерпала дневную квоту Kinopoisk: догрузка запрашивает полные данные каждого фильма страницы топа, включая уже известные, а после неудачи перезапускается на каждом запросе (design.md, Context; Decisions 4, 7, 8). Раздел 11 это исправляет; 9.1 повторяется после него.

## 11. Экономия квоты Kinopoisk и пауза после неудачной догрузки (design.md, Decisions 4, 7, 8)

- [x] 11.1 `movie_service.get_top_movies_from_kinopoisk`: пропускать фильмы, чей `kinopoisk_id` уже есть в БД, до запроса полных данных (Decision 7).
- [x] 11.2 `config.py`: `CATALOG_GROWTH_COOLDOWN_MINUTES: int = 30`.
- [x] 11.3 `deck_service`: после пустой или упавшей фоновой догрузки ставить паузу; `_maybe_grow_catalog` не запускает догрузку до её истечения; постановка паузы логируется одной строкой (Decision 8).
- [x] 11.4 402 от Kinopoisk логировать одной строкой `WARNING` без traceback (Decision 4).
- [x] 11.5 Тест: для фильмов страницы топа, уже существующих в БД, полные данные не запрашиваются (счётчик вызовов замоканного `fetch_movie_from_kinopoisk`).
- [x] 11.6 Тест: после пустой догрузки повторный запуск не происходит в пределах паузы и происходит после её истечения.
- [x] 11.7 Прогнать весь `pytest tests` — зелёный.

## 9. Верификация

- [x] 9.1 Ручная проверка с реальным `KINOPOISK_API_KEY`: замерить время ответа `GET /api/movies/random` в момент пересечения порога `DECK_CATALOG_LOW_THRESHOLD` — убедиться, что ответ приходит быстро (не ждёт Kinopoisk), а в логах видно последующее появление новых фильмов через `replenished-from-catalog` на следующих запросах.
- [x] 9.2 Ручная проверка сессии: искусственно состарить `swiped_at` существующих свайпов комнаты (например, через SQL) на ≥24 часа и без новых свайпов от участников убедиться, что ранее просвайпанный фильм снова доступен к показу; затем убедиться, что случайное совпадение лайков из старой сессии не создаёт матч.
