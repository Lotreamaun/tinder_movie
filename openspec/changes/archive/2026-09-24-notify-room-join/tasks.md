## 1. Notification service

- [x] 1.1 Добавить в `backend/app/services/notification_service.py` метод `send_room_join_notification(self, room, joined_user, db)`: синхронная точка входа, извлекающая из ORM-объектов чистые данные (код комнаты, список `telegram_id` остальных участников, имя присоединившегося, итоговое число участников) до запуска фонового потока.
- [x] 1.2 Реализовать асинхронный метод отправки (по образцу `_send_match_notification_async`): свой `SessionLocal()` при необходимости, цикл по получателям с `await self.bot.send_message(chat_id=..., text=...)` в собственном `try/except TelegramError` на каждого получателя, текст — plain text без `parse_mode`.
- [x] 1.3 Не отправлять ничего и не создавать фоновый поток, если список получателей (участники, кроме присоединившегося) пуст.
- [x] 1.4 Обработать случай `self.bot is None` (нет токена) так же, как в существующих методах — без исключений, просто ничего не отправлять.

## 2. Интеграция с присоединением к комнате

- [x] 2.1 В `backend/app/services/room_service.py::RoomService.join_room` вызвать `notification_service.send_room_join_notification(room, user, db)` сразу после `db.commit()`/`db.refresh(room)`.
- [x] 2.2 Обернуть вызов в `try/except Exception` с `logger.error(..., exc_info=True)`, чтобы ошибка уведомления не приводила к сбою самого присоединения к комнате.

## 3. Тесты

- [x] 3.1 Добавить unit-тесты для `send_room_join_notification` (мок `telegram.Bot.send_message`): уведомление уходит каждому из остальных участников; присоединившийся не получает уведомление о себе; ошибка отправки одному получателю не мешает отправке остальным.
- [x] 3.2 Добавить тест на имя/юзернейм со спецсимволами Markdown-разметки (`_`, `*`, `` ` ``, `[`) — уведомление доставляется без ошибки.
- [x] 3.3 Добавить тест на присоединение к комнате без других участников — фоновая отправка не запускается (получателей нет).
- [x] 3.4 Добавить/дополнить тест на `RoomService.join_room`, что успешное присоединение не откатывается и не падает, даже если отправка уведомления кидает исключение.

## 4. Ручная проверка

- [ ] 4.1 Локально прогнать сценарий: создать комнату, присоединиться вторым пользователем через `/join_room`, убедиться, что первый участник получает сообщение о присоединении в Telegram.
