## 1. Исправление обработчика `/room_info`

- [x] 1.1 В `backend/app/bot/handlers.py` убрать `parse_mode='Markdown'` из `update.message.reply_text(...)` в обработчике `room_info` (отправлять как обычный текст)
- [x] 1.2 Проверить и при необходимости поправить оформление сообщения (эмодзи-заголовки, код комнаты), чтобы оно осталось читаемым без Markdown-разметки

## 2. Тесты

- [x] 2.1 Добавить тест(ы) на формирование/отправку сообщения `/room_info` для участника, чей `first_name` или `username` содержит спецсимволы Markdown (`_`, `*`, `` ` ``, `[`), проверяющий отсутствие ошибки отправки
- [x] 2.2 Добавить тест на комнату с 3+ участниками, подтверждающий, что `/room_info` отрабатывает так же надёжно, как с 1-2 участниками

## 3. Проверка

- [x] 3.1 Прогнать `pytest` полностью, убедиться в отсутствии регрессий
- [x] 3.2 Вручную (или через тест) убедиться, что `create_room`/`join_room` по-прежнему используют `parse_mode='Markdown'` без изменений и без регрессий
