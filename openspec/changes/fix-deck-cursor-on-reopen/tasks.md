## 1. Backend: откат позиции

- [ ] 1.1 Добавить в `deck_service.py` константу `DECK_REWIND_LOOKBACK = 20` с комментарием (связь с очередью фронта и порогом рецикла)
- [ ] 1.2 Добавить в `get_next_movie_for_room` параметр `rewind: bool = False`; при `True` после взятия блокировки позиции и вычисления `swiped` откатить `position.position` по правилу design.md, Decision 2 (окно по вычищенной колоде, последний свайпнутый `movie_id` → `i + 1`, иначе начало окна, никогда не вперёд), затем обычный scan в той же транзакции
- [ ] 1.3 Пробросить `rewind` через `movie_service.get_random_movie`
- [ ] 1.4 Добавить в `GET /api/movies/random` необязательный query-параметр `rewind: bool = False` (учитывается только вместе с `room_code`), обновить докстринг ручки

## 2. Backend: тесты

- [ ] 2.1 Тест: выдано A, B, C, D, свайпнут A → запрос с `rewind` возвращает B, следующие без признака — C, D, затем фильм после D
- [ ] 2.2 Тест: все выданные свайпнуты → `rewind` возвращает тот же фильм, что и без признака
- [ ] 2.3 Тест: в окне нет свайпов → откат к началу окна, не дальше; пропуски старше окна не возвращаются
- [ ] 2.4 Тест: запрос без `rewind` позицию назад не двигает (существующие тесты последовательной и параллельной выдачи остаются зелёными)
- [ ] 2.5 Тест: колода с дублями после рецикла — повторное вхождение свайпнутого фильма считается границей окна
- [ ] 2.6 Прогнать `pytest` целиком

## 3. Frontend

- [ ] 3.1 `services/api.ts`: параметр `rewind?: boolean` в `getRandomMovie`, в query как `rewind=true` только если он задан
- [ ] 3.2 `App.tsx`: в `initializeApp` первый запрос начальной очереди идёт с `rewind: true`, остальные и `loadMoreMovies` — без; обновить комментарий про StrictMode и продвижение курсора
- [ ] 3.3 `npm run build` и `npm run lint` без ошибок

## 4. Проверка вручную

- [ ] 4.1 В Telegram: посвайпать, закрыть мини-приложение при непустой очереди, открыть снова — первой показывается карточка, бывшая на экране, затем фильмы из очереди; в логах бэкенда число выдач минус число свайпов между запусками не копится
